# Generated by `apm pack` from apm.yml.
# Prettier reformats JSON arrays in ways APM does not match,
# which makes the CI staleness check (apm pack && git diff --quiet)
# always fail. Let APM own the format of its own output.
.claude-plugin/marketplace.json

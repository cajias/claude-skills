# teaching-double-parsingn-a

Learned from user correction: Teaching: double-parsing)\n- architectural why comments are appropriate and should remain\n- the computed variables in `resolveandvalidateprotocols` are all used\n\nthe file is clean after 2 rounds of simplification | Correction: User wants 'stop' instead of 'yet performed' | User need: 'to' | Expected behavior: 'remain\n- the computed variables in `resolveandvalidateprotocols` are all used\n\nthe file is clean after 2 rounds of simplification'

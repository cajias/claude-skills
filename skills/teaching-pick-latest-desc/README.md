# teaching-pick-latest-desc

Learned from user correction: Teaching: pick latest\" --description \"$(cat <<'eof'\n## summary\n- the pypi simple index doesn't guarantee chronological ordering of package links\n- `tail -1` picked the march 31 wheel (`0 | Correction: User wants 'stop' instead of 'yet performed' | User need: 'to' | User wants instead: 'april 1 release (`0' | Expected behavior: 'i also'

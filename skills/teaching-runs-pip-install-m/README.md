# teaching-runs-pip-install-m

Learned from user correction: Teaching: runs `pip install` manually | Correction: User wants 'from terminal' instead of 'easily callable' | User need: 'to' | Avoid: 'runs `pip install` manually' | Expected behavior: 'i also'

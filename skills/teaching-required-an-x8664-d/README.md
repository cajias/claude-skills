# teaching-required-an-x8664-d

Learned from user correction: Teaching: required an x86_64 docker image | Correction: User wants 'hence' instead of 'present' | User need: 'to' | Avoid: 'worked\*\* — you may have previously deployed via `cdk deploy` or ci' | Expected behavior: 'i also'

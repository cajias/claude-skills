# teaching-exposed-it-as-a-job

Learned from user correction: Teaching: exposed it as a job variable, consumer-level overrides like `anthropic_model: 'opus'` were silently ignored | Correction: Avoid: 'exposed it as a job variable'

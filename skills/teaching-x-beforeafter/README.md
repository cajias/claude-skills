# teaching-x-beforeafter

Learned from user correction: Teaching: x\", \"before/after x\") require hooks configured in settings | Correction: User wants 'any tasks' instead of 'work on' | User need: 'to' | Avoid: 'work on any tasks' | Expected behavior: 'i also'

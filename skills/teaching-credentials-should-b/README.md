# teaching-credentials-should-b

Learned from user correction: Teaching: credentials should be assumed via `isengardcli run --` (don't put credentials in the | Correction: User wants 'credentials or' instead of 'put aws' | User need: 'to' | Avoid: 'put credentials in the' | Expected behavior: 'be assumed via `isengardcli run --` (don't put credentials in the'

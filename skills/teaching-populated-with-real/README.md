# teaching-populated-with-real

Learned from user correction: Teaching: populated with real values | Correction: User wants 'configured' instead of 'properly' | User need: 'to' | Avoid: 'populated with real values' | Expected behavior: 'i also'

# teaching-set-workflow-rule-n

Learned from user correction: Teaching: set, workflow rule never matched\n\nnow we need the python-gitlab fix | Correction: User wants 'stop' instead of 'yet performed' | User need: 'to' | Avoid: 'set, workflow rule never matched\n\nnow we need the python-gitlab fix' | Expected behavior: 'i also'

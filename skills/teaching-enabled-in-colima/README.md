# teaching-enabled-in-colima

Learned from user correction: Teaching: enabled in colima** (it requires both `vmtype: vz` and `rosetta: true`, but the config shows `vmtype: qemu` and `rosetta: false`) | Correction: User wants 'the' instead of 'ship with' | Avoid: 'enabled in colima** (it requires both `vmtype: vz` and `rosetta: true`' | Expected behavior: 'cause docker to pull the amd64 variant of `rust:1'

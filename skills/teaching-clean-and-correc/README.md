# teaching-clean-and-correc

Learned from user correction: Teaching: ```clean and correct way to disable v1 without removing the component include | Correction: User wants 'so this' instead of 'cpu bound' | User wants instead: 'of claude-opus-4-6 model id 4' | Avoid: 'runs. --- ### issues found #### minor: mr description is stale the mr description claims changes include: - "added`except: [triggers]`to all non-agent-mr jobs" - "added`$ci_pipeline_source != "trigger"` to deploy:gamma rules" neither of these is in the actual diff' | Expected behavior: 'be updated to match reality'

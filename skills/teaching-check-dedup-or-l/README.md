# teaching-check-dedup-or-l

Learned from user correction: Teaching: `--check-dedup` or let the compile pipeline handle it | Correction: User wants 'the regular' instead of 'part of' | User need: 'to' | Avoid: 'over-engineer or assume features work that don't' | Expected behavior: 'i also'

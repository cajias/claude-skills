# teaching-cdk-directly-for-dep

Learned from user correction: Teaching: cdk directly for deployment - source `| Correction: User wants 'repo root' instead of 'found in' | Avoid: 'use cdk directly for deployment - source`'

# teaching-pass-quality-gates

Learned from user correction: Teaching: pass quality gates | Correction: User wants 'pass' instead of 'absolute' | User need: 'to' | Avoid: 'pass quality gates' | Expected behavior: 'handle test fixture updates better |\n| quality gate rejects pre-existing failures | no baseline diff — it compares against \"all pass\" | quality gate should compare against baseline'

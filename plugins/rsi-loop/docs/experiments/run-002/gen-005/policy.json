{
  "algorithm": "adversarial-probe-tree-search",
  "num_drafts": 5,
  "max_nodes": 9,
  "model": "haiku",
  "effort": "low",
  "context_mode": "full-history",
  "selection": "public-search-then-shared-adversarial-probe-tiebreak; probe pool guarantees the improve/explore-lineage leaves and scales with the tie count (bounded), with strongest drafts as brittle contrast",
  "probe_topk": 4,
  "probe_topk_max": 8,
  "public_tie_band": 0.05,
  "min_spread": 0.1,
  "max_escalations": 1,
  "probe_reserve_tokens": 350000,
  "draft_directions": [
    "simplest correct baseline — prioritize validity over quality",
    "sorting/preprocessing-based heuristic (e.g. handle the hardest elements first)",
    "incremental best-choice heuristic: at each step take the locally optimal placement/decision",
    "deterministic local search: start from a greedy solution, then iteratively improve it",
    "robustness-first: parse/interpret the input tolerantly (accept rephrasings, synonyms, reordered clauses, formatting and encoding variation) and normalize before solving, so the same intent always maps to the same answer"
  ]
}

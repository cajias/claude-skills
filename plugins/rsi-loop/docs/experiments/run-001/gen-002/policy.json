{
  "algorithm": "aide2-family-diverse-tree-search",
  "num_drafts": 5,
  "max_nodes": 9,
  "model": "haiku",
  "effort": "low",
  "context_mode": "full-history for improve/debug; code-blind (families+scores+summaries only) for explore",
  "selection": "lexicographic (public, stress); explore-on-public-tie-or-stall; greedy improve otherwise",
  "stress_harness": true,
  "draft_directions": [
    "family 'naive-baseline': simplest correct baseline — prioritize validity over quality",
    "family 'sorted-greedy': order the input once, then place each element by a fixed greedy rule",
    "family 'complement-matching': build the solution by explicitly pairing/grouping complementary elements that combine tightly — NOT a one-pass greedy placement",
    "family 'local-search-repack': start from a trivial valid solution, then apply deterministic improvement moves (relocate, swap, merge, re-split) until a fixpoint — the move engine is the point, and it must actually change the solution it is given",
    "family 'exact-hybrid': solve small subproblems exactly (bounded DP / branch-and-bound / exhaustive over small subsets, with strict cutoffs) and fall back safely on large inputs"
  ]
}

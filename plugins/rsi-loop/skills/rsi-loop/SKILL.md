---
name: rsi-loop
description: "PLACEHOLDER — not yet implemented. AIDE²-style recursive self-improvement outer loop: propose a rewrite of the inner research agent, evaluate on a heterogeneous task battery under a fixed budget, keep only if it beats the incumbent on private held-out scores. Do not invoke yet; see docs/PLAN.md in this plugin."
---

# rsi-loop (placeholder)

This skill is a stub. The implementation plan — including the outer-loop protocol, the AIDE0
baseline inner agent, the public/private evaluation split, reward-hacking defenses, and the
RSI-ladder measurement commands — is specified in [`docs/PLAN.md`](../../docs/PLAN.md).

If invoked before implementation lands: explain that the plugin is in design phase and point the
user to the plan.
